{
  "providers": {
    "project": {
      "baseUrl": "${MODEL_BASE_URL}",
      "api": "openai-completions",
      "apiKey": "${MODEL_API_KEY}",
      "headers": { "x-opencode-session": "${MODEL_SESSION_ID}" },
      "models": [
        { "id": "${MODEL_ID}" }
      ]
    }
  }
}
